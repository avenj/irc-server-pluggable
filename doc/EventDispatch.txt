IRC -> Filter -> Backend
  { ircsock_* event post }

    -> Dispatcher (ISA Emitter)
    { process() / emit() }

      -> Protocol  <-> [ registered consumer sessions ]
      { ->dispatcher->dispatch() }

    <- Dispatcher
    { dispatch() yields send() }

IRC <- Filter <- Backend
